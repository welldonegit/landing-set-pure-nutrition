/**
 * Каталог для замовлення в KeyCRM. Єдине довірене джерело ціни — тут, на
 * сервері. Ціну з фронтенду не приймаємо: усе, що в бандлі, підробне.
 *
 * Ключ — точне значення <option> у формі. Незнайоме значення size —
 * помилка валідації, а не мовчазна підстановка.
 */
export const PRODUCTS = {
  'Дрібна порода (сет 3×300 г)': { sku: '250420217', price: 495 },
  'Середня / велика порода (сет 3×1 кг)': { sku: '250420218', price: 1289 },
};

/** { sku, price } за точним значенням size або null. */
export const productForSize = (size) => PRODUCTS[size] ?? null;

/** Апсел «смаколики з качкою» — окрема позиція, коли клієнт увімкнув тумблер. */
export const UPSELL = { sku: '5220232352562', price: 220 };

